# WB 贴图 — 问题与解决方法

更新日期：2026-06-21
迁移自：`E:\wb-new-arrivals\workspace\WB-Material\`

---

## 已修复的问题

### 1. 路径 `\1` `\2` 被解释为八进制转义
**问题**：`'D:\Semems\1AI\DX0001\12.png'` 中 `\1` `\2` 被Python解释为SOH/STX。
**解决**：用 `chr(92).join(['D:', 'Semems', '1AI', ...])` 构建路径。

### 2. 美图未关闭时重复点击AI工具/AI贴图
**问题**：美图还在运行就重复点AI工具→AI贴图，新增图片按钮被关闭。
**解决**：美图未关闭时直接点新增图片，不点AI工具和AI贴图。

### 3. 尾数1从右下角拖动导致图片缩放
**问题**：从右下角拖动了尾数1的贴图，导致拉伸变小。
**解决**：尾数1必须从**贴图内部**(2100,720)开始拖，加Alpha偏移。

### 4. 首次打开胚衣图片大小不对
**问题**：第一次打开美图时胚衣显示大小异常。
**解决**：关图→不保存→重新打开胚衣，图片大小就正常了。

### 5. 黑背尾数1误加了旋转
**问题**：黑背尾1也和白背一样做了旋转，位置异常。
**解决**：黑背尾1**不旋转**，直接选中→拖动→混合→保存。

### 6. 剪贴板用subprocess clip不生效
**问题**：`subprocess.run(['clip'], input=path.encode())` 无法正确设置剪贴板。
**解决**：用 `powershell -Command Set-Clipboard` → 最终改用 **pyperclip**。

### 7. 保存路径最终方案（v2.2关键修复）
**根因**：保存弹窗是Qt DirectUI控件，普通路径设置不生效。
**解决**：
1. pyperclip 替代 PowerShell Set-Clipboard
2. 双重验证：粘贴前读取确认 + 粘贴后读取确认
3. Ctrl+V 分步慢速（0.05/0.03/0.03/0.1秒）
4. 先删旧文件避免覆盖弹窗干扰

### 8. patch工具破坏代码结构
**问题**：使用patch工具替换click函数时，代码变成了嵌套定义，所有点击失效。
**解决**：每次patch后必须编译检查和功能测试。

---

## 未解决问题

暂无。
