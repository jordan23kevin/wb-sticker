# WB 贴图 — 快速运行手册 v2.3.0

## 触发词

用户说 **"WB 贴图"**

## 运行命令

```bash
# 白T 指定文件夹
uv run --with pillow --with numpy --with pyperclip --with pywin32 python wb_sticker.py DX0020

# 黑T 指定文件夹
uv run --with pillow --with numpy --with pyperclip --with pywin32 python wb_sticker.py --black DX0020
```

## 停止

用户说 **"x"** 或 **"停止"**，立即杀进程。

## 前置条件

1. 美图秀秀已安装
2. DX 文件夹有 PNG 素材（由 WB去背生成）
3. 胚衣在 `D:\Semems\1胚衣\`

## 成功输出（基线）

```
DX0020 白T:
  白T 尾数1 — DX0020/4BW.png    ← 背
    [verify] old: D:\Semems\1AI\DX0020  ← 路径正确
    ✅ xxxKB                            ← 保存成功
  白T 尾数2 — DX0020/4BW.png    ← 正
    ✅ xxxKB                            ← 保存成功

DX0020 黑T:
  黑T 尾数1 — DX0020/4BW.png    ← 背
    [verify] old: D:\Semems\1AI\DX0020
    ✅ xxxKB
  黑T 尾数2 — DX0020/4BW.png    ← 正
    ✅ xxxKB
```

## 语法检查

```bash
uv run --with pillow --with numpy --with pyperclip --with pywin32 python -m py_compile wb_sticker.py
```

## 版本历史

| 版本 | 日期 | 说明 |
|:----|:----|:-----|
| v2.3.0 | 2026-06-21 | ✅ 成功基线 DX0020，保存路径+验证全部修复 |
| v2.0 | 2026-06-16 | 初始版（wb-new-arrivals） |
